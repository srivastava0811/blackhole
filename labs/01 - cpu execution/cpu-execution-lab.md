# CPU Execution Lab 
### September 14th - 18th, 2026

This lab is a demonstration of written program code's relationship with the CPU of our computers. Written code turns into smaller blocks of work that live in the CPU, which include [**machine instructions**](https://pages.cs.wisc.edu/~markhill/cs354/Fall2008/notes/basics.html), [**memory reads/writes**](https://pages.cs.wisc.edu/~markhill/cs354/Fall2008/notes/basics.html), [**cache behavior**](https://www.cs.cornell.edu/courses/cs3410/2024fa/notes/caches.html), and [**performance differences**](https://www.cs.cornell.edu/courses/cs3410/2025sp/notes/pipelining.html). We write and showcase all of this in the form of constructing basic matrix multiplication (matmul), since this computation is fundamental in Machine Learning workloads and hardware accelerator designs. 

Matmuls also work quite well for showing how data moves differently based on simple changes within the operation itself. Tenstorrent's Blackhole, then, can be thought of as a later extension of these same ideas, but at scale, with topics that this lab should serve as a foundation for: 
- [**local memory near compute**](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/labs/matmul/lab1/lab1.html), 
- [**many parallel workers**](https://docs.tenstorrent.com/aibs/blackhole/installation.html), 
- [**explicit data movement**](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/apis/kernel_apis/data_movement/data_movement.html), 

and 

- [**specialized hardware for ML-shaped computation**](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/advanced_topics/compute_engines_and_dataflow_within_tensix.html). 

---

# CPU Execution Lab: Setup, Workflow, and Submission Guide

This guide takes you from a new computer setup to a complete individual pull request for Lab 1. Every student should implement the full lab in their own fork of the repository. Your pull request will contain your code, while the pull request description will serve as your lab report.

The instructor will review the individual implementations, results, and explanations and may combine the strongest ideas into the shared benchmark and final main program.

## 1. What This Lab Is About

This lab asks one central question:

> How much can performance change when the computation stays the same but the data moves differently?

You will investigate that question through:

- strided array access;
- row-first and column-first matrix traversal;
- memory-copy bandwidth;
- naive matrix multiplication;
- matrix multiplication using a transposed copy of B;
- serial and threaded array summation; and
- selected assembly generated by the compiler.

This is an introductory systems lab, but it also establishes ideas that will return later in the Blackhole project: data layout, local memory, explicit data movement, parallel workers, regular matrix computation, and the relationship between source code and machine execution.

## 2. What You Need

Every student needs:

1. A GitHub account.
2. A personal fork of the repository.
3. Git.
4. Visual Studio Code.
5. A terminal.
6. A C compiler that accepts the command cc.
7. POSIX thread support through pthreads.

The supported development environments are:

| Computer | Supported lab environment |
| --- | --- |
| macOS | Terminal with Apple Clang |
| Linux | A normal Linux terminal with GCC or Clang |
| Windows | Ubuntu through Windows Subsystem for Linux, or WSL |

Windows students should compile and run the lab inside Ubuntu/WSL, not through native PowerShell, Command Prompt, Visual Studio, or MinGW. This gives the class one mostly consistent terminal and pthread environment.

You do not need:

- CMake;
- Tenstorrent hardware;
- TT-Metal;
- Docker;
- Python packages; or
- a GPU.

## 3. First Setup Check

Open the terminal for your supported environment:

- macOS: open Terminal.
- Linux: open your normal terminal.
- Windows: open Ubuntu after completing the WSL instructions below.

Run:

~~~sh
git --version
cc --version
~~~

Both commands should print version information. The exact version numbers and compiler names may differ between computers.

You should also confirm that Visual Studio Code opens normally.

### Configure Your Git Identity

Git attaches a name and email address to your commits. If you have never configured them, run:

~~~sh
git config --global user.name "Your Name"
git config --global user.email "YOUR-EMAIL"
~~~

Use an email connected to your GitHub account, or use your GitHub-provided private email if you do not want your personal email shown in commits.

Check the saved values:

~~~sh
git config --global user.name
git config --global user.email
~~~

## 4. Install the Required Tools

### macOS

Install Apple's Command Line Tools:

~~~sh
xcode-select --install
~~~

A system window should appear. Complete the installation, reopen Terminal if necessary, and verify:

~~~sh
git --version
cc --version
~~~

The cc command normally invokes Apple Clang on macOS.

Apple's official instructions are available in [Installing the command-line tools](https://developer.apple.com/documentation/xcode/installing-the-command-line-tools).

Install [Visual Studio Code](https://code.visualstudio.com/) if it is not already installed.

### Linux

On Ubuntu or Debian, run:

~~~sh
sudo apt update
sudo apt install git build-essential
~~~

Then verify:

~~~sh
git --version
cc --version
~~~

The build-essential package installs the common compiler, headers, and build tools needed for this lab. Other Linux distributions may use another package manager, but the required outcomes are the same: Git, a C compiler, standard C headers, and pthread support.

Install [Visual Studio Code](https://code.visualstudio.com/) if it is not already installed.

### Windows Through WSL

For this lab, Windows students will use Ubuntu through WSL.

#### Step 1: Check Whether WSL Is Already Installed

Open PowerShell or Windows Terminal and run:

~~~powershell
wsl --status
wsl --list --verbose
~~~

If Ubuntu appears in the distribution list, open Ubuntu from the Start menu and continue to Step 3.

#### Step 2: Install WSL and Ubuntu

Open PowerShell as Administrator and run:

~~~powershell
wsl --install
~~~

Restart the computer if Windows asks you to do so. Then open Ubuntu from the Start menu.

If WSL exists but Ubuntu is missing, run:

~~~powershell
wsl --install -d Ubuntu
~~~

The first Ubuntu launch asks you to create a Linux username and password. The password remains invisible while you type; that is normal terminal behavior.

Microsoft's current WSL setup guidance is available in [Set up a WSL development environment](https://learn.microsoft.com/en-us/windows/wsl/setup/environment).

#### Step 3: Install the Lab Tools Inside Ubuntu

Run these commands in the Ubuntu terminal, not PowerShell:

~~~sh
sudo apt update
sudo apt install git build-essential
~~~

Then verify:

~~~sh
git --version
cc --version
~~~

#### Step 4: Connect VS Code to WSL

Install Visual Studio Code on Windows and install Microsoft's **WSL** extension in VS Code. The compiler and repository will live inside Ubuntu, while the VS Code window runs normally on Windows.

Later, after cloning the repository inside Ubuntu, open it with:

~~~sh
code .
~~~

The first launch may install a small VS Code server inside WSL. When the folder opens, the lower-left corner of VS Code should indicate that the window is connected to WSL or Ubuntu. A terminal opened with **Terminal > New Terminal** should also be an Ubuntu shell.

See [Developing in WSL](https://code.visualstudio.com/docs/remote/wsl) for Microsoft's complete VS Code workflow.

## 5. Fork the Shared Repository

Do not begin by cloning the instructor's repository directly. First create your own GitHub fork.

1. Sign in to GitHub.
2. Open [rvfvn/blackhole](https://github.com/rvfvn/blackhole).
3. Select **Fork** near the upper-right corner.
4. Keep your personal GitHub account as the owner.
5. Keep the repository name **blackhole**.
6. Create the fork.

A fork is your own GitHub copy of the shared repository. You can push your work there without changing the instructor's repository. You will later use a pull request to propose your completed lab.

GitHub explains this model in [Work with forks](https://docs.github.com/en/pull-requests/how-tos/work-with-forks).

## 6. Clone Your Fork

Open your fork on GitHub. Its address should begin with your username:

~~~text
https://github.com/YOUR-USERNAME/blackhole
~~~

Select **Code**, choose **HTTPS**, and copy the displayed URL.

In your terminal, move to the location where you keep projects and run:

~~~sh
git clone https://github.com/YOUR-USERNAME/blackhole.git
cd blackhole
~~~

Replace YOUR-USERNAME with your actual GitHub username. Do not type the placeholder literally.

Windows students should run these commands inside Ubuntu. A simple location is the WSL home directory:

~~~sh
cd ~
git clone https://github.com/YOUR-USERNAME/blackhole.git
cd blackhole
~~~

Confirm that origin points to your fork:

~~~sh
git remote -v
~~~

Both origin entries should contain your GitHub username. If they show only rvfvn/blackhole, you cloned the instructor repository instead of your fork.

GitHub's general cloning instructions are available in [Cloning a repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/cloning-a-repository).

## 7. Open the Repository in VS Code

Open the complete **blackhole** repository folder, not only one C file and not only the src folder.

### Through the VS Code Menus

1. Open Visual Studio Code.
2. Select **File > Open Folder**.
3. Find the cloned **blackhole** folder.
4. Select the folder and open it.
5. Confirm that the repository's README and labs folder appear in the Explorer.
6. Select **Terminal > New Terminal**.

### From the Terminal

If the code command is available, enter the repository and run:

~~~sh
code .
~~~

The integrated terminal normally opens at the root of the folder currently open in VS Code. Confirm your location:

~~~sh
pwd
git status
~~~

Windows students should confirm that VS Code is connected to WSL before compiling.

## 8. Synchronize the Fork Before Working

The instructor may add or revise starter files after you create your fork. Updating the instructor repository does not automatically update your fork.

Use this beginner-friendly two-part process.

### Part A: Update the Fork on GitHub

1. Open your fork on GitHub.
2. Select **Sync fork**.
3. Select **Update branch** if GitHub reports that your fork is behind.

### Part B: Update the Clone on Your Computer

In the VS Code terminal, from the repository root, run:

~~~sh
git switch main
git pull origin main
~~~

The first command places you on your fork's main branch. The second downloads the newly synchronized files from your GitHub fork.

A plain git pull checks origin, which is your fork. That is why the GitHub **Sync fork** step happens first.

Before starting the lab, confirm that this directory exists:

~~~text
labs/01 - cpu execution
~~~

Do not create your work branch until the fork and local main branch are current.

## 9. Create Your Individual Work Branch

Every student will submit one individual pull request containing their own complete implementation.

From the repository root, run:

~~~sh
git switch main
git pull origin main
git switch -c lab01-YOUR-USERNAME
~~~

Replace YOUR-USERNAME with your GitHub username.

Check the active branch:

~~~sh
git branch --show-current
~~~

It should print your lab branch, not main. Complete the lab on this branch. Do not make lab commits directly on main.

## 10. Enter and Understand the Lab Directory

From the repository root:

~~~sh
cd "labs/01 - cpu execution"
~~~

The quotation marks matter because the folder name contains spaces.

The important structure is:

~~~text
01 - cpu execution/
├── build.sh
├── cpu-execution-lab.md
├── include/
│   └── bench.h
└── src/
    ├── main.c
    ├── timer.c
    ├── machine_info.c
    ├── stride_access.c
    ├── row_col_access.c
    ├── bandwidth_copy.c
    ├── matmul_naive.c
    ├── matmul_transposed.c
    └── threaded_sum.c
~~~

The responsibilities are deliberately separated:

- **bench.h** is the shared menu of public function declarations.
- **timer.c** supplies the monotonic benchmark clock.
- **machine_info.c** reports the environment in which results were measured.
- Each experiment file performs one focused computation.
- **main.c** allocates data, selects sizes, calls experiments, times them, checks results, prints comparisons, and releases memory.

Keep the existing public function names and argument lists. Stable function signatures make it possible to compare and integrate implementations from many individual pull requests.

## 11. Build the Starter Program

From inside the lab directory, run:

~~~sh
cc -std=c11 -O2 -Wall -Wextra -pedantic -Iinclude src/*.c -pthread -o cpu_lab
~~~

The standard command includes pthread support because the threaded sum is required for everyone.

The Session 1 slides showed the build command before pthread became a standard requirement. For the repository as it now exists, use the command in this guide.

The options mean:

| Option | Purpose |
| --- | --- |
| cc | Run the system C compiler |
| -std=c11 | Compile using the C11 language standard |
| -O2 | Enable normal compiler optimization |
| -Wall | Enable a broad group of useful warnings |
| -Wextra | Enable additional warnings |
| -pedantic | Report code that does not follow the selected standard closely |
| -Iinclude | Search the include directory for bench.h |
| src/*.c | Compile every C source file in src |
| -pthread | Enable and link POSIX thread support |
| -o cpu_lab | Name the resulting executable cpu_lab |

A successful compilation commonly prints nothing and returns to the prompt. Warnings and errors should be read rather than ignored.

You may also use the provided shortcut:

~~~sh
sh build.sh
~~~

The direct cc command remains the reference command because it makes every build choice visible.

## 12. Run the Program

After a successful build:

~~~sh
./cpu_lab
~~~

The initial starter may finish without printing benchmark results because main.c and the experiment functions are student skeletons. That is not a setup failure. Output will appear as you implement and connect the stages.

Rebuild after changing source code:

~~~sh
cc -std=c11 -O2 -Wall -Wextra -pedantic -Iinclude src/*.c -pthread -o cpu_lab
./cpu_lab
~~~

Do not assume that an old cpu_lab executable contains your newest changes. Compilation creates a new executable from the current source files.

## 13. Recommended Implementation Chronology

Everyone should attempt the full lab. The order below follows the concepts introduced by the source files and lets each experiment build on the previous one.

### Step 1: Read bench.h

Start with **include/bench.h**. It shows the public functions available across the program.

Do not change a function name, return type, or argument list merely to make an implementation easier. The header is a shared contract between main.c and the experiment files.

### Step 2: Read timer.c

The timer is prewritten infrastructure. Understand:

- why it uses a monotonic clock;
- why it returns a double measured in seconds;
- how main.c can calculate elapsed time as end minus start; and
- why allocation, initialization, and printing usually belong outside a timed region.

You should use benchmark_time_seconds() rather than adding unrelated timing methods to individual experiment files.

### Step 3: Read machine_info.c

The machine-information function is also prewritten infrastructure. Understand why benchmark results need context:

- operating environment;
- CPU architecture;
- compiler;
- compiler version; and
- logical CPU core count.

Call this function from main.c so the terminal output copied into your pull request identifies the machine used.

### Step 4: Implement stride_access.c

This is the first experiment implementation.

Your function should:

- read every array element exactly once;
- change the order of those reads according to the stride;
- return a checksum as a double;
- avoid allocation, timing, and printing; and
- handle the edge conditions described in the file.

Test several strides while keeping the amount of useful data and arithmetic comparable.

### Step 5: Implement row_col_access.c

Implement both matrix traversal functions:

- sum_matrix_by_rows();
- sum_matrix_by_columns().

Both should read the same flat row-major matrix and return the same mathematical sum, allowing for a possible small floating-point difference. Only the traversal order changes.

This is the clearest bridge between one-dimensional memory addresses and the way a two-dimensional matrix is visited.

### Step 6: Implement bandwidth_copy.c

Implement the element-by-element copy.

Keep the copying function focused on reads and writes. Validate the destination and calculate effective bandwidth in main.c, outside the timed function.

Use:

~~~text
useful bytes moved = 2 * length * sizeof(float)
effective GB/s = useful bytes moved / elapsed seconds / 1000000000.0
~~~

The factor of two represents one useful read from the source and one useful write to the destination.

### Step 7: Implement matmul_naive.c

Implement the direct three-loop definition of square matrix multiplication.

For each output C[i,j], accumulate:

~~~text
A[i,k] * B[k,j]
~~~

over k.

Keep allocation, initialization, timing, validation, and performance reporting in main.c.

An approximate operation count is:

~~~text
2 * n * n * n floating-point operations
~~~

From this, main.c can calculate approximate GFLOP/s.

### Step 8: Implement matmul_transposed.c

First implement the function that creates a separate transposed copy of B. Then implement the matmul that reads B's original columns as neighboring values in the transposed storage.

The result must still be A multiplied by B. It is not a different mathematical multiplication.

Measure the transpose cost separately from the multiplication so your report can distinguish:

- preparation cost;
- kernel time; and
- the possible benefit when the same prepared matrix is reused.

### Step 9: Implement threaded_sum.c

Implement:

- the serial baseline;
- the private pthread worker;
- the division of the array into non-overlapping ranges;
- thread creation;
- thread joining;
- partial-sum combination; and
- cleanup of thread-related allocations.

Every worker should write to its own partial sum. Do not have every thread repeatedly update one shared sum.

Compare the serial and threaded results for correctness before interpreting speedup:

~~~text
speedup = serial elapsed seconds / threaded elapsed seconds
~~~

Threading is not guaranteed to make a small workload faster. Thread creation, scheduling, joining, and result combination all have costs.

### Step 10: Complete main.c

Use the stages already placed in main.c as the integration checklist.

Main should eventually:

1. print machine information;
2. select array sizes, matrix sizes, strides, repetitions, and thread counts;
3. allocate and initialize inputs;
4. run each experiment;
5. measure elapsed time;
6. verify results;
7. calculate useful metrics;
8. print readable comparisons; and
9. free every successful allocation.

The experiment functions should remain focused. Main is where the complete benchmark is assembled.

### After Each Step

Do not wait until every file is finished before compiling.

A useful cycle is:

1. implement one focused function;
2. remove only the temporary unused-parameter lines replaced by your implementation;
3. compile with all warnings enabled;
4. fix errors and warnings;
5. run any available correctness check;
6. inspect your changes; and
7. make a meaningful commit.

## 14. Benchmarking Expectations

A benchmark is more than code that prints a time.

### Verify Correctness First

A faster incorrect implementation is not a successful result.

Use checksums or output comparisons so that:

- stride variants read all expected elements;
- row and column traversals agree;
- the copy destination matches the source;
- both matmul versions produce matching output matrices; and
- serial and threaded sums agree within a reasonable floating-point tolerance.

### Keep Setup Work Outside the Timed Region

Unless you are intentionally measuring setup cost, do not include these inside a kernel timing:

- memory allocation;
- input initialization;
- result printing;
- correctness checking; or
- unrelated file operations.

Transpose time is important, but report it separately from the transposed matmul kernel.

### Run More Than Once

One run can be affected by other programs, scheduling, temperature, power settings, and cache state. Test each important case more than once and state how you summarized repeated measurements.

Possible summaries include:

- minimum time;
- median time; or
- arithmetic mean.

Whichever method you choose, document it in the pull request.

### Compare Fairly

When comparing two versions:

- use the same inputs;
- use the same compiler command;
- use the same optimization level;
- test on the same machine;
- perform equivalent correctness checks; and
- keep the amount of mathematical work comparable.

### Interpret Before Comparing Computers

First compare patterns on your own machine. Absolute times from two students may differ because their processors, memory systems, compilers, operating environments, and background workloads differ.

The main result is the relationship between variants, not a contest for the smallest number.

## 15. Generate and Inspect Assembly

To generate optimized assembly for one experiment:

~~~sh
cc -std=c11 -O2 -Iinclude -S src/stride_access.c -o stride_access.s
~~~

You can replace stride_access.c with another experiment file.

Look for:

- register names;
- load instructions;
- store instructions;
- arithmetic instructions;
- comparisons;
- branches; and
- repeated instruction groups corresponding to loops.

Assembly differs between ARM64 and x86-64 and can differ between compiler versions. You are not expected to memorize exact instructions.

The repository ignores generated .s files. Include your observations in the pull-request description instead of committing generated assembly unless the instructor specifically asks for it.

## 16. Save Your Work with Git

Return to the repository root when using the examples below:

~~~sh
cd ../..
~~~

Check your location and branch:

~~~sh
git status
git branch --show-current
~~~

Inspect changes before staging:

~~~sh
git diff
git diff --check
~~~

The second command helps locate whitespace mistakes.

Stage only intentional work. For example:

~~~sh
git add "labs/01 - cpu execution/src/stride_access.c"
git commit -m "Implement strided array traversal"
~~~

Continue making focused commits as you complete the lab. Other useful commit messages might be:

~~~text
Implement row and column matrix traversal
Add memory copy bandwidth experiment
Implement naive and transposed matmul
Add pthread array sum
Integrate CPU benchmarks in main
~~~

Before submission, inspect the final state:

~~~sh
git status
git log --oneline --max-count=10
~~~

Do not commit generated files such as:

- cpu_lab;
- cpu_lab.exe;
- object files;
- generated assembly; or
- editor-specific temporary files.

The repository's .gitignore already excludes the common forms of these artifacts, but git status is still your final check.

## 17. Push Your Branch

Push the personal lab branch to your fork:

~~~sh
git push -u origin lab01-YOUR-USERNAME
~~~

Replace YOUR-USERNAME with the same username used in your branch name.

The -u option connects the local branch to the branch on your fork. Later pushes from the same branch can usually use:

~~~sh
git push
~~~

## 18. Open the Individual Pull Request

After pushing:

1. Open your fork on GitHub.
2. Select **Compare & pull request** if the banner appears.
3. If needed, open the original [rvfvn/blackhole](https://github.com/rvfvn/blackhole) repository and choose to create a pull request across forks.
4. Confirm the destination:
   - base repository: **rvfvn/blackhole**
   - base branch: **main**
5. Confirm the source:
   - head repository: **YOUR-USERNAME/blackhole**
   - compare branch: **lab01-YOUR-USERNAME**
6. Use the title:

~~~text
Lab 01: CPU execution - YOUR NAME
~~~

7. Leave **Allow edits by maintainers** enabled when GitHub offers that option.
8. Complete the pull-request description using the required report below.
9. Select **Create pull request**.

GitHub's official instructions are in [Creating a pull request](https://docs.github.com/en/pull-requests/how-tos/create-pull-requests/creating-a-pull-request).

Submit one pull request per student. Do not combine several students' implementations into one submission.

## 19. Required Pull-Request Report

Your pull-request description is your lab write-up. Do not create a separate shared results file unless the instructor later requests one.

Paste relevant terminal output as text inside fenced code blocks. Include the commands that produced the output. A successful compiler command may print nothing; in that case, state clearly that it completed without warnings or errors.

Use this structure:

~~~~markdown
## Student

Name:
GitHub username:
Computer and operating environment:

## Implementation Summary

Briefly explain what you implemented across the lab.

## Files Changed

List the source files you changed and the purpose of each change.

## Build Command and Compiler Output

Paste the complete compiler command.

~~~text
$ cc -std=c11 -O2 -Wall -Wextra -pedantic -Iinclude src/*.c -pthread -o cpu_lab
PASTE COMPILER OUTPUT HERE
~~~

State whether the build completed with warnings or errors.

## Machine Information

Paste the output produced by machine_info().

~~~text
PASTE MACHINE INFORMATION HERE
~~~

## Benchmark Configuration

Document:

- array sizes;
- matrix sizes;
- stride values;
- repetition count;
- thread counts;
- compiler and optimization level; and
- how repeated measurements were summarized.

## Terminal Output and Results

Paste the important output from ./cpu_lab.

~~~text
$ ./cpu_lab
PASTE BENCHMARK OUTPUT HERE
~~~

Summarize the measurements in tables when useful.

### Strided Access

| Array size | Stride | Time | Checksum or status |
| --- | ---: | ---: | --- |
| | | | |

### Row and Column Traversal

| Matrix size | Traversal | Time | Checksum or status |
| --- | --- | ---: | --- |
| | | | |

### Copy Bandwidth

| Array size | Time | Effective GB/s | Validation |
| --- | ---: | ---: | --- |
| | | | |

### Matrix Multiplication

| Matrix size | Version | Time | Approximate GFLOP/s | Validation |
| --- | --- | ---: | ---: | --- |
| | | | | |

Report transpose time separately.

### Serial and Threaded Sum

| Array size | Threads | Time | Speedup | Validation |
| --- | ---: | ---: | ---: | --- |
| | | | | |

## Analysis

Answer these questions in your own words:

1. How did increasing stride change performance, and why?
2. How did row traversal compare with column traversal?
3. What does the copy experiment suggest about the cost of moving data?
4. Did transposing B change matmul performance?
5. When the transpose cost is included, how does your interpretation change?
6. When did threading help, and when did its coordination cost matter?
7. Which results were surprising or inconsistent?
8. How do these CPU observations motivate specialized accelerators such as Blackhole?

## Assembly Notes

State which source file you compiled to assembly. Identify examples of registers, loads, stores, arithmetic, branches, or loop structure. Exact instruction names may differ by machine.

## Problems, Decisions, and Lab Notes

Describe:

- bugs or compiler warnings you encountered;
- how you corrected them;
- implementation choices you made;
- remaining limitations;
- questions you still have; and
- anything the instructor should consider when integrating the final benchmark.

## Checklist

- [ ] I implemented every experiment.
- [ ] I completed main.c.
- [ ] I built with -O2, warnings, and -pthread.
- [ ] The final build has no compiler errors.
- [ ] I investigated or resolved compiler warnings.
- [ ] I checked correctness before discussing performance.
- [ ] I included machine information.
- [ ] I included terminal output and benchmark results.
- [ ] I included my analysis and notes.
- [ ] I did not commit generated executables or assembly.
~~~~

Replace every placeholder. Do not submit an empty table or leave PASTE OUTPUT HERE in the final report.

## 20. Respond to Review

The instructor will review your code and report. A review may request corrections, additional measurements, or a clearer explanation.

To respond:

1. Return to your existing lab branch.
2. Make the requested changes.
3. Rebuild and rerun the relevant experiments.
4. Commit the correction.
5. Push the same branch.

For example:

~~~sh
git switch lab01-YOUR-USERNAME
git add "labs/01 - cpu execution/src" "labs/01 - cpu execution/include"
git commit -m "Address Lab 01 review feedback"
git push
~~~

The existing pull request updates automatically when you push to its branch. Do not open a second pull request for review corrections.

The instructor may integrate, adapt, or compare parts of several submissions rather than merging one student's complete main.c unchanged. Your clear function boundaries, benchmark evidence, and written reasoning make that synthesis possible.

## 21. Troubleshooting

### cc: command not found

Return to the platform setup:

- macOS: install the Xcode Command Line Tools.
- Ubuntu, Linux, or WSL: install build-essential.

### pthread.h Is Missing

Windows students should confirm they are compiling inside Ubuntu/WSL rather than PowerShell or a native Windows terminal. Linux/WSL students should confirm that build-essential installed successfully.

### Undefined References to pthread Functions

Confirm that the build command includes:

~~~text
-pthread
~~~

Use the complete command from this guide.

### bench.h Cannot Be Found

Confirm that:

- you are inside the lab directory;
- the include directory exists; and
- the command contains -Iinclude.

Use:

~~~sh
pwd
ls
ls include
ls src
~~~

### src/*.c Cannot Be Found

You are probably in the wrong folder. Enter:

~~~sh
cd "labs/01 - cpu execution"
~~~

from the repository root.

### The Program Runs but Prints Nothing

The starter main.c is a skeleton. Connect the stages and add deliberate reporting as you implement the lab.

### Timings Change Between Runs

Some variation is normal. Repeat the measurements, reduce heavy background activity, document your summary method, and focus on patterns rather than one isolated number.

### VS Code Shows Red Underlines but cc Compiles Successfully

VS Code's code-analysis configuration can differ from the actual terminal compiler. The strict terminal build is the final authority for this lab. You can still correct VS Code's include-path settings later.

### git push Asks for Authentication

Follow GitHub's browser or credential-manager prompts. GitHub does not accept an account password as a command-line Git password over HTTPS.

### The Pull Request Targets the Wrong Repository

Before submitting, recheck:

~~~text
base: rvfvn/blackhole, branch main
compare: YOUR-USERNAME/blackhole, branch lab01-YOUR-USERNAME
~~~

## 22. Final Submission Checklist

Before requesting review, confirm all of the following:

- Your fork was synchronized before you created the work branch.
- Your branch contains your individual implementation.
- Every required source function is implemented.
- main.c runs every experiment.
- The project builds with the documented command and -pthread.
- Compiler errors are gone and warnings have been investigated.
- Results are checked for correctness.
- Timing setup is described.
- Machine information is included.
- Terminal output is included in the pull-request description.
- Your analysis explains the results rather than only listing numbers.
- Assembly observations are included.
- Generated executables and assembly files are not committed.
- The PR targets rvfvn/blackhole main.
- The PR is ready for instructor review.

The final goal is not for every student to produce identical code or identical timing. The goal is for each student to produce a correct, explainable experiment and enough evidence for the class to understand how source code, machine execution, data movement, and parallel work relate.

## References and Source Notes

The first group below records sources that are cited directly in this guide or in the Session 1 slides. The later groups contain authoritative references that support the technical explanations in the starter code and lab workflow. Those supporting references were reconstructed from the subjects covered by the lab; they should not be interpreted as a claim that they are the exact pages originally consulted while every comment was written.

### Sources Cited Directly in the Lab or Slides

- [Session 1 slide deck](./CPU-Execution-Lab-Session-1.pptx.pdf)
- [University of Wisconsin-Madison CS 354: Machine Organization Basics](https://pages.cs.wisc.edu/~markhill/cs354/Fall2008/notes/basics.html) - instructions, registers, memory reads and writes, and the basic stored-program model.
- [Cornell CS 3410: Caches](https://www.cs.cornell.edu/courses/cs3410/2024fa/notes/caches.html) - cache hierarchy, cache lines, locality, and memory-access behavior.
- [Cornell CS 3410: Pipelining and CPU Performance](https://www.cs.cornell.edu/courses/cs3410/2025sp/notes/pipelining.html) - instruction execution and CPU performance.
- [Wikipedia: Central processing unit](https://en.wikipedia.org/wiki/Central_processing_unit) - introductory CPU description used on the CPU slide.
- [GeeksforGeeks: Central Processing Unit](https://www.geeksforgeeks.org/computer-science-fundamentals/central-processing-unit-cpu/) - CPU components and the fetch, decode, execute, and store explanation and image used in the slides.
- [Algorithmica: External Memory](https://en.algorithmica.org/hpc/external-memory/hierarchy/) - memory-hierarchy illustration used in the slides.
- [DataChant: Excel Matrix Multiplication](https://datachant.com/2016/06/01/excel-matrix-multiplication-replacing-mmult-with-power-query/) - matrix-multiplication example cited on the slides.
- [GeeksforGeeks: How to Multiply 2 x 2 Matrices](https://www.geeksforgeeks.org/maths/how-to-multiply-2-x-2-matrices/) - introductory matrix-multiplication example cited on the slides.
- [Pinterest: Matrix Multiplication Image](https://ru.pinterest.com/pin/477029785519648105/) - image provenance recorded in the slides; this is not used as a technical authority.

### Setup, Git, and Development Environment

- [GitHub: Work with forks](https://docs.github.com/en/pull-requests/how-tos/work-with-forks) - the relationship between the class repository and each student's fork.
- [GitHub: Syncing a fork](https://docs.github.com/en/pull-requests/how-tos/work-with-forks/syncing-a-fork) - bringing upstream changes into a fork.
- [GitHub: Cloning a repository](https://docs.github.com/en/repositories/creating-and-managing-repositories/cloning-a-repository) - creating the local clone.
- [GitHub: Creating a pull request](https://docs.github.com/en/pull-requests/how-tos/create-pull-requests/creating-a-pull-request) - submitting individual implementations and lab reports for review.
- [Visual Studio Code](https://code.visualstudio.com/) - editor download.
- [Visual Studio Code: Terminal Basics](https://code.visualstudio.com/docs/terminal/basics) - running the lab commands in the integrated terminal.
- [Visual Studio Code: Developing in WSL](https://code.visualstudio.com/docs/remote/wsl) - opening the Linux-side repository correctly on Windows.
- [Microsoft: Set Up a WSL Development Environment](https://learn.microsoft.com/en-us/windows/wsl/setup/environment) - installing and configuring Ubuntu through WSL.
- [Apple: Installing the Command-Line Tools](https://developer.apple.com/documentation/xcode/installing-the-command-line-tools) - installing Apple Clang and command-line development tools on macOS.
- [Ubuntu: build-essential Package](https://packages.ubuntu.com/search?keywords=build-essential) - the compiler and standard build-tool package used by Ubuntu and WSL setup.

### C, POSIX, Threads, and Compiler Behavior

- [cppreference: C Arrays](https://en.cppreference.com/c/language/array) - contiguous array elements, multidimensional arrays, and row-major layout.
- [cppreference: C Memory Model and Data Races](https://en.cppreference.com/c/language/memory_model) - shared memory, conflicting accesses, and data-race behavior.
- [cppreference: memcpy](https://en.cppreference.com/c/string/byte/memcpy) - the standard C memory-copy operation and its non-overlap requirement.
- [The Open Group: clock_gettime and CLOCK_MONOTONIC](https://pubs.opengroup.org/onlinepubs/000095399/functions/clock_getres.html) - monotonic timestamps and `struct timespec` timing.
- [GNU C Library: Feature Test Macros](https://sourceware.org/glibc/manual/latest/html_node/Feature-Test-Macros.html) - why `_POSIX_C_SOURCE 200809L` exposes POSIX.1-2008 interfaces on systems using glibc.
- [The Open Group: pthread_create](https://pubs.opengroup.org/onlinepubs/000095399/functions/pthread_create.html) - creating a POSIX worker thread and passing its argument.
- [The Open Group: pthread_join](https://pubs.opengroup.org/onlinepubs/9799919799.2024edition/functions/pthread_join.html) - waiting for a worker to finish before using its result.
- [The Open Group: sysconf](https://pubs.opengroup.org/onlinepubs/9699919799/functions/sysconf.html) - querying operating-system configuration values.
- [Linux man-pages: sysconf](https://man7.org/linux/man-pages/man3/sysconf.3.html) - the `_SC_NPROCESSORS_ONLN` query used by `machine_info.c`.
- [GCC: Overall Options](https://gcc.gnu.org/onlinedocs/gcc/Overall-Options.html) - compilation stages and producing assembly with `-S`.
- [GCC: Optimize Options](https://gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html) - transformations enabled by optimization levels, including loop and basic-block vectorization at `-O2`.
- [Clang Command Guide](https://clang.llvm.org/docs/CommandGuide/clang.html) - Clang compilation stages, `-S`, language-standard selection, and optimization levels.
- [David Goldberg: What Every Computer Scientist Should Know About Floating-Point Arithmetic](https://docs.oracle.com/cd/E19957-01/806-3568/ncg_goldberg.html) - rounding behavior and why equivalent accumulation orders can differ slightly.

### Memory Access and Numerical Performance

- [Intel 64 and IA-32 Architectures Optimization Reference Manual, Volume 1](https://cdrdv2-public.intel.com/821612/248966-Optimization-Reference-Manual-V1-050.pdf) - cache locality, cache-line traffic, prefetching, and the performance cost of large-stride access.
- [STREAM Benchmark](https://www.cs.virginia.edu/stream/ref.html) - the established model for measuring sustained memory bandwidth with simple array operations.
- [Netlib BLAS: GEMM](https://www.netlib.org/lapack/explore-html/dd/d09/group__gemm.html) - the standard general matrix-matrix multiplication operation.
- [UT Austin LAFF: Matrix-Matrix Multiplication](https://www.cs.utexas.edu/~flame/laff/laff/LAFF.pdf) - matrix multiplication, data reuse, and the approximately `2n^3` floating-point-operation count.
- [Berkeley Lab: Roofline Performance Model](https://amcr.lbl.gov/departments/computer-science-department/ppan/roofline-performance-model/) - the relationship among computation, memory traffic, bandwidth, and attainable performance.

### Tenstorrent and Blackhole

- [Tenstorrent: Installing Blackhole PCIe Cards](https://docs.tenstorrent.com/aibs/blackhole/installation.html) - Blackhole installation material cited in the original lab introduction.
- [Tenstorrent: Blackhole PCIe Cards](https://docs.tenstorrent.com/aibs/blackhole/) - current Blackhole card specifications, Tensix-core count, SRAM, RISC-V cores, GDDR6 capacity, and memory bandwidth.
- [TT-Metalium: Memory from a Kernel Developer's Perspective](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/advanced_topics/memory_for_kernel_developers.html) - shared on-core SRAM, private RISC-V memory, and the memory visible to kernels.
- [TT-Metalium Lab 1: Single-Core Matrix Multiplication](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/labs/matmul/lab1/lab1.html) - Tensix cores, the NoC, local SRAM, DRAM movement, and a device matrix-multiplication example.
- [TT-Metalium: Data Movement APIs](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/apis/kernel_apis/data_movement/data_movement.html) - explicit NoC reads, writes, multicast, barriers, and local-memory operations.
- [TT-Metalium: Compute Engines and Data Flow within Tensix](https://docs.tenstorrent.com/tt-metal/latest/tt-metalium/tt_metal/advanced_topics/compute_engines_and_dataflow_within_tensix.html) - RISC-V control, data flow between SRAM and registers, and specialized matrix and vector engines.
